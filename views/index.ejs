<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10">
            <form action="/create" method="post">
                <input class="block px-5 py-2 bg-zinc-800 rounded-md w-full outline-none text-white" name="title" type="text"
                    placeholder="Title goes here...">
                <textarea class="block px-5 resize-none py-2 mt-2 rounded-md w-full text-white outline-none bg-zinc-800"
                    name="details" placeholder="write your task details" id=""></textarea>
                <input class="bg-blue-600 hover:cursor-pointer hover:bg-blue-500 transition-all px-5 py-2 rounded-md mt-2 text-white font-semibold" type="submit"
                    value="Create Note">
            </form>
        </div>
        <div class="tasks p-10 flex gap-3 flex-wrap">
            <% if (files && files.length > 0) { %>
                <% files.forEach(function(task) { %>
                    <div class="task w-72 min-w-min px-3 py-4 rounded-md bg-zinc-800">
                        <h1 class="text-white text-3xl tracking-tighter"><%=task %></h1>
                        <div class="flex justify-between">
                        <a href="/files/<%=task %>" class=text-blue-600 inline-block mt-3">Read More</a>
                        <a href="/edit/<%=task%>" class="text-zinc-600">edit filename</a>
                        <a href="/delete/<%=task%>" class="text-zinc-600">Delete</a>
                    </div>
                    </div>
                <% }); %>
            <% } else { %>
                <h3 class="text-zinc-500">No tasks here...</h3>
            <% } %>
        </div>
    </div>
    <script src="/javscripts/script.js"></script>
</body>

</html>